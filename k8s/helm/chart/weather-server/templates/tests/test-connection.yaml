apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weather-server.fullname" . }}-test-connection"
  labels:
    {{- include "weather-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weather-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
